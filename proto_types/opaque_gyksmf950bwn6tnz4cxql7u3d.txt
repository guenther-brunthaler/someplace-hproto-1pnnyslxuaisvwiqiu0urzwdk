"opaque" (UUID gyksmf950bwn6tnz4cxql7u3d) - A string containing binary bytes that are not necessarily text but may as well be such. It is a BLOB (binary large object) or BOB (binary object) of data without known (or cared about) internal structure. This is the recommended fallback for passing through arbitrary data, totally ignoring its internal representation or alignment requirements).
